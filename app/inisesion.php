<?php

	/*require_once("login.php"); //Se asegura que el usuario este autenticado

	$atributos = $saml->getAttributes(); //Obtiene sus atributos

	$cuenta = $atributos['uCuenta'][0];

	$user = User::find($cuenta);
	
	Auth::login($user);*/